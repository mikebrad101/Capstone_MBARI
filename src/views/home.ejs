<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MBARI Homepage</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
  rel="stylesheet" 
  integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
  crossorigin="anonymous">
  <link href="/css/styles.css" rel="stylesheet">

</head>
<body>

  <h1>MBARI Expedition Database Dashboard</h1>
    <div class="center-container">
        <form>
            <table class="button-table">
                <tr>
                   <td><a href="/login" class="button">Login</a></td>
                    <td><a href="/allcruises" class="button">All Cruises</a></td>
                  </tr>
                  <tr>
                    <td><a href="/preexp" class="button">New Cruise</a></td>
                    <td><a href="/dive" class="button">Dive</a></td>
                  </tr>
                  <tr>
                    <td><a href="/postexp" class="button">Update Cruise</a></td>
                    <td><a href="/search" class="button">Search</a></td>




                  <% if (role == 'Mbari Employee') { %>
                    <a href="/home" class="button">Home</a>
                  <% } %>
                  %>
                 <!-- <% if (role === 'Mbari employee' || role === 'Registered User') { %>
                    <td><a href="/preexp" class="button">New Cruise</a></td>
                  <% } %>
                  %>
                  <% if (role === 'Logistics Coordinator') { %>
                    <td><a href="/allcruises" class="button">All Cruises</a></td>
                  <% } %>
    
                  %>
                  <tr>
                  <% if (role === 'Mbari employee' || role === 'Registered User') { %>
                    <td><a href="/postexp" class="button">Update Cruise</a></td>
                  <% } %>
                  %>
                  <% if (role === 'Logistics Coordinator') { %>
                    <td><a href="/search" class="button">Search</a></td>
                  <% } %>
                  %>       
                  -->           

                </tr>
                <!-- Add more rows with buttons as needed -->
            </table>
        </form>
    </div>
</body>
</html>